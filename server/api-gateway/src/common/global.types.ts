export type AuthUser = {
    accountId: string;
    userId: string;
    email: string;
    deviceId: string;
}

export enum ELanguage {
    PYTHON = 'python',
    NODE_JS = 'node-js',
    C = "c",
    CPP = "cpp",
    REACT_JS = "react-js",
    REACT_TS = "react-ts",
    NEXT_TS = "next-ts"
}